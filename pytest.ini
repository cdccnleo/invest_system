[pytest]
testpaths = tests
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (requires PostgreSQL + credentials)
    slow: Slow tests (LLM calls, large datasets)