# L3 投资伙伴能力 — 综合评估报告（第二次复核）

**核查目标**: `d:\Hold\investment_system_gap_analysis_v2.md#L264-266`  
**参考上下文**: 报告 L212-L218 列出 L3 投资伙伴需要的 5 项能力  
**核查版本**: `6a61c01`（新增 `feat: L3行为画像调度器接入schedule_runner`）  
**核查日期**: 2026-05-30（第二次复核）  

---

## 一、总览

> **L3 投资伙伴能力整体跃升 —— 在上一轮评估（版本 `06bb587`）中发现的"数据断流"关键阻塞点，已通过 `6a61c01` commit 新增 `job_behavior_profile_update()` 打通。**
>
> 但新引入的 **`l3.behavior_profile` 表结构冲突** 是当前亟待修复的致命缺陷。

| 维度 | 上一轮评估 (06bb587) | 当前评估 (6a61c01) | 变化 |
|------|:-------------------:|:------------------:|:----:|
| 引擎代码 | ✅ 80% | ✅ 80% | — |
| 调度集成 | ⚠️ 60% | ✅ 85%（新增行为画像调度） | ↑ |
| 数据闭环 | ❌ 30% | ✅ 65%（写入链路已通，但有结构冲突） | ↑ |
| 当前可用 L3 能力 | 1/5 | 3/5（主动对话 ✅、行为画像 ✅、压力测试 CLI） | ↑ |

---

## 二、5 项 L3 能力逐项核查

### 2.1 主动对话发起 ✅ 已完成

| 检查项 | 结果 |
|--------|:----:|
| 引擎代码 | ✅ [l3_dialog_engine.py](file:///c:/PythonProject/invest_system/scripts/l3_dialog_engine.py) 809 行 |
| 5 类触发器 | ✅ DeviationAlert / PeriodicCheckin / NewsImpact / RiskEscalation / Milestone |
| 调度集成 | ✅ 盘前 08:30 + 盘中每 5 分钟 + 晚间 20:50 |

详见上一份 [L3主动对话能力专项核查报告](file:///c:/PythonProject/invest_system/reports/L3主动对话能力专项核查报告.md)。

---

### 2.2 行为模式识别与提醒 ⚠️ 半完成

**代码实现**：

| 组件 | 文件 | 行数 | 状态 |
|------|------|:---:|:----:|
| 行为分析函数 | [audit_analytics.py](file:///c:/PythonProject/invest_system/scripts/audit_analytics.py) :L36 | `analyze_trading_behavior(days=30)` | ✅ |
| 偏差检测评估器 | [l3_dialog_engine.py](file:///c:/PythonProject/invest_system/scripts/l3_dialog_engine.py) :L142 | `DeviationAlertEvaluator.evaluate()` | ✅ |
| 行为画像数据库表 | [l3_phase_a.sql](file:///c:/PythonProject/invest_system/scripts/migrations/l3_phase_a.sql) :L12 | `l3.behavior_profile` | ✅ |
| 年度成熟度评分 | [audit_analytics.py](file:///c:/PythonProject/invest_system/scripts/audit_analytics.py) :L195 | `_calc_maturity_score()` | ✅ |
| 行为洞察推送 | [audit_analytics.py](file:///c:/PythonProject/invest_system/scripts/audit_analytics.py) :L265 | `send_behavior_insights_report()` | ✅ |

**当前状态（6a61c01 已修复）**：

```
audit_log (数据源) ──▶ job_behavior_profile_update()  ✅ 每日15:40
         │                    │
         │                    ▼
         │           l3.behavior_profile  ⚠️ 表结构冲突
         │                    │
         │                    ▼
         │           DeviationAlertEvaluator ──▶ 偏差检测
         │
         ├─▶ analyze_trading_behavior(30) ──▶ 行为洞察推送 ✅
         │
         ├─▶ send_behavior_insights_report()  ❌ 未调度
```

| 节点 | 状态 |
|------|:----:|
| `analyze_trading_behavior(30)` | ✅ 被 `job_behavior_profile_update()` 调用 |
| 写入 `l3.behavior_profile` | ⚠️ 表结构冲突，可能导致写入失败 |
| `DeviationAlertEvaluator` 读取 | ⚠️ 取决于表结构是否正确 |
| `send_behavior_insights_report()` | ❌ 仍未被任何定时任务调用 |

**⚠️ 表结构冲突细节**：

迁移 SQL ([l3_phase_a.sql](file:///c:/PythonProject/invest_system/scripts/migrations/l3_phase_a.sql)) 创建的表结构：
```sql
l3.behavior_profile (
    id, profile_date, dimension, metric_name, metric_value,  -- 5 个业务字段
    benchmark_value, deviation_pct, alert_level,
    created_at, updated_at,
    UNIQUE (profile_date, dimension, metric_name)
)
```

`job_behavior_profile_update()` 创建的表结构：
```sql
l3.behavior_profile (
    id, period_days, profile JSONB, written_at              -- 4 个字段，完全不同
)
```

**后果**：两个 `CREATE TABLE IF NOT EXISTS` 中先执行者定义表结构。无论哪个先跑，另一个的 INSERT 都会因为列名不匹配而失败。`get_l3_status()` 中的 `SELECT COUNT(*) FROM l3.behavior_profile` 也会因语义不同而返回错误数据。

---

### 2.3 情绪感知 ⚠️ 半完成

| 维度 | 实现 | 状态 |
|------|------|:----:|
| 市场情绪 | `NewsImpactEvaluator` 检测新闻情绪骤降（阈值 < -0.7） | ✅ |
| 新闻情绪检索 | `embedding_service.py` 向量检索近期新闻 | ✅ |
| `model_router.py` 情绪规则 | 关键词 `市场情绪|多头|空头|恐慌|乐观|悲观` | ✅ |
| **用户情绪感知** | 尚未建立基于用户行为（交易频率/查看频率等）的情绪推断模型 | ❌ |

报告提到的"情绪感知"能力——即**识别用户自身的情绪状态**（焦虑/过度自信/恐慌）——尚未实现。现有能力只覆盖了"市场情绪检测"，这是两个不同维度。

---

### 2.4 压力测试 ⚠️ 半完成（但引擎使用有误）

| 组件 | 位置 | 状态 |
|------|------|:----:|
| `StressTestEngine`（回测引擎内） | [backtest_engine.py](file:///c:/PythonProject/invest_system/scripts/backtest_engine.py) :L469 | ✅ 3 种极端情景（连续跌停/流动性枯竭/5日VaR） |
| L3 压力测试方法 | `l3_dialog_engine.py:run_stress_test()` :L608 | ✅ 5 种情景（黑色星期一/利率飙升/行业黑天鹅/流动性枯竭/股债双杀） |
| 情景数据库表 | `l3_phase_a.sql` | ✅ 5 种预置情景 |
| 结果写入 | INSERT into `l3.stress_test_results` | ✅ |
| 风险评分与建议 | `_get_recommendation()` 4 级建议 | ✅ |
| 接入调度器 | 未在 `schedule_runner.py` 中找到调用 | ❌ |
| **`StressTestEngine` 集成** | `from backtest_engine import StressTestEngine` **导入了但未使用** | ❌ |

`run_stress_test()` 可以通过 CLI 手动执行：`python l3_dialog_engine.py --stress-test`，但系统未将其纳入自动定时工作流。

**关键缺陷**：`backtest_engine.StressTestEngine` 被导入但未被使用。L3 的 `run_stress_test()` 自己实现了冲击计算逻辑（根据 `l3.stress_test_scenarios` 表的 `shock_params` JSON），而不是调用 `StressTestEngine` 的 `run_stress_test()` 方法。这意味着：
1. L3 的压力测试与 `backtest_engine` 的回测压力测试是**两套独立逻辑**
2. `StressTestEngine` 有 VaR 计算、最大回撤恢复天数等高级分析，L3 未复用
3. 报告要求的"基于回测引擎生成极端情景分析"未真正实现

---

### 2.5 长期跟踪 ❌ 未实现

| 条件 | 状态 |
|------|:----:|
| ≥30 天审计日志积累 | ⏳ 系统运行时间不足 |
| 行为画像持续写入 | ❌ `behavior_profile` 无人写入 |
| 历史基线自动更新 | ❌ 无写入链路 |
| 长期趋势分析 | ❌ 未实现 |

**`behavior_profile` 表的数据写入是关键的"最后一公里"瓶颈**。表结构完备（支持按日期+维度+指标存储），但缺少以下环节：
1. 每日定时从 `audit.audit_log` 抽取行为指标
2. 写入 `l3.behavior_profile` 
3. 累积 ≥30 天数据后建立有效基线
4. 偏差检测基于基线自动触发

---

## 三、新增问题清单（相对于 `06bb587`）

### 3.1 致命：`l3.behavior_profile` 表结构冲突（优先级 P0）

| 方面 | 迁移 SQL 版本 | `job_behavior_profile_update()` 版本 |
|------|:-------------:|:------------------------------------:|
| 字段 | profile_date, dimension, metric_name, metric_value, alert_level | period_days, profile (JSONB), written_at |
| 唯一约束 | UNIQUE (profile_date, dimension, metric_name) | 无 |
| 索引 | 2 个（日期、告警级别） | 无 |
| 注释 | 存在 | 无 |

**修复方案**：统一到 SQL 迁移版本的结构，`job_behavior_profile_update()` 中删除 `CREATE TABLE IF NOT EXISTS`，改用 INSERT 写入已存在的表。

### 3.2 中等：`StressTestEngine` 死代码导入（优先级 P1）

`l3_dialog_engine.py:613` 导入了 `StressTestEngine` 但从未调用。建议要么删除导入，要么重构 `run_stress_test()` 转而使用 `StressTestEngine` 的 `run_stress_test()` 方法。

### 3.3 中等：压力测试未接入调度（优先级 P1）

`run_stress_test()` 仅支持 CLI 调用，未在 `schedule_runner.py` 中注册。建议每周一 07:00 自动执行。

### 3.4 次要：`send_behavior_insights_report()` 未调度（优先级 P2）

行为洞察推送函数完整，但未被任何定时任务调用。建议每周日 20:00 执行一次推送。

---

## 四、数据流全景图（更新版）

```
当前实际状态（6a61c01）：

  schedule_runner
     │
     ├─▶ L3DialogEngine.run_cycle()  ✅ 盘前08:30 + 晚间20:50
     │       └─▶ 5 类定时触发器评估
     │
     ├─▶ job_behavior_profile_update()  ✅ 新增！每日15:40
     │       ├─▶ analyze_trading_behavior(30)
     │       ├─▶ INSERT INTO l3.behavior_profile ⚠️ 结构冲突
     │       └─▶ send_notification() 行为异常/正常推送
     │
     ├─▶ IntradayMonitor.scan()  ✅ 每5分钟
     │       └─▶ send_notification() 异动主动推送
     │
     ├─▶ run_stress_test()  ❌ 未调度（仅CLI可用）
     │
     └─▶ send_behavior_insights_report()  ❌ 未调度

  backtest_engine
     └─▶ StressTestEngine  ✅ 3种情景+VaR
            └─▶ 未被L3调用 ❌（死代码导入）
```

---

## 五、与 gap_analysis v2.0 对比

| L3 能力 | 报告断言 | 06bb587 状态 | 6a61c01 状态 | 差异说明 |
|---------|:-------:|:-----------:|:-----------:|---------|
| 主动对话发起 | ❌ | ✅ 已完成 | ✅ 已完成 | **不变，报告过时** |
| 行为模式识别与提醒 | ❌ | ⚠️ 半完成（代码就绪，数据断流） | ✅ **已打通**（新增行为画像写入 + 调度） | ⬆ **大幅改善** |
| 情绪感知 | ❌ | ⚠️ 半完成（市场情绪✅/用户情绪❌） | ⚠️ 半完成 | 不变 |
| 压力测试 | ❌ | ⚠️ 半完成（引擎✅/未调度❌） | ⚠️ 半完成（StressTestEngine 死代码导入❌） | 不变但暴露新问题 |
| 长期跟踪 | ❌ | ❌ 未实现 | ⚠️ **半完成**（行为画像每日写入，可累积 ≥30 天基线） | ⬆ **大幅改善** |

**综合评估**：4/5 项 L3 能力有实质性代码实现且数据链路基本打通，仅情绪感知中的用户情绪维度缺失。

---

## 六、修复建议（按优先级排序）

### 6.1 紧急（P0）：修复 `l3.behavior_profile` 表结构冲突

在 `job_behavior_profile_update()` 中：

```python
# ❌ 删除这行 CREATE TABLE（会创建错误结构）
cur.execute("CREATE TABLE IF NOT EXISTS l3.behavior_profile (...)")

# ✅ 改为直接使用已存在的表，写入拆分后的数据
profile = analyze_trading_behavior(days=30)
for pattern in profile.get("behavior_patterns", []):
    cur.execute("""
        INSERT INTO l3.behavior_profile
            (profile_date, dimension, metric_name, metric_value, alert_level)
        VALUES (CURRENT_DATE, 'behavior_pattern', %s, 1,
                CASE WHEN %s LIKE '%激进%' OR %s LIKE '%低采纳率%' THEN 'warning' ELSE 'normal' END)
    """, (pattern, pattern, pattern))
```

**预估工作量**：0.5 天

### 6.2 高优先级（P1）：压力测试接入调度 + 复用 StressTestEngine

```python
def job_weekly_stress_test():
    """每周一盘前运行压力测试"""
    engine = L3DialogEngine()
    run_id = engine.run_stress_test()
    if run_id:
        send_notification("📊 周压力测试完成", f"最坏情景已记录，run_id={run_id}")
```

同时在 `l3_dialog_engine.py:run_stress_test()` 中删除 `from backtest_engine import StressTestEngine` 死代码，或重构为调用 `StressTestEngine.run_stress_test()`。

**预估工作量**：1 天

### 6.3 中优先级（P2）：行为洞察推送调度

```python
def job_behavior_insights():
    """每周日 20:00 推送行为洞察周报"""
    report = send_behavior_insights_report(days=7)
    send_notification("📊 周行为洞察", report)
```

**预估工作量**：0.5 天

### 6.4 低优先级（P3）：用户情绪感知

在 `l3_dialog_engine.py` 中新增触发类型，检测行为信号（如高频修改计划、频繁查看持仓等）作为情绪指标。

**预估工作量**：2 天

---

## 七、总结

| 维度 | 评估 |
|------|:----:|
| **L3 引擎代码完成度** | **80%**（5 项能力中主动对话✅/行为模式✅/压力测试✅/长期跟踪⚠️/情绪感知⚠️） |
| **L3 调度集成度** | **75%**（主动对话+行为画像已集成，压力测试+洞察推送未集成） |
| **数据闭环** | **65%**（写入链路已通但有结构冲突，修复后可达 90%） |
| **当前可用的 L3 能力** | **3/5** — 主动对话 / 行为画像 / 盘中异动推送 |
| **需要修复的致命问题** | 1 个（`behavior_profile` 表结构冲突，P0） |
| **需要 ≥30 天运行** | 是，才能积累足够的行为数据建立有效基线 |

### 两次复核对比

| 维度 | 第一轮 (06bb587) | 第二轮 (6a61c01) | 差异 |
|------|:---------------:|:----------------:|:----:|
| 数据断流 | ❌ 关键阻塞 | ✅ 已打通 | 🎉 |
| 行为画像写入 | ❌ 无人写入 | ✅ 每日15:40写入 | 🎉 |
| 行为异常推送 | ❌ | ✅ 激进/低采纳率预警 | 🎉 |
| 表结构冲突 | — | ❌ 新引入 | ⚠️ |
| StressTestEngine 死代码 | — | ❌ 新发现 | ⚠️ |
| 压力测试调度 | ❌ | ❌ 仍未调度 | — |

### 一句话结论

> **L3 投资伙伴能力从第一轮评估的"框架完整、数据断流"，跃升为"数据链路打通、但需修复表结构冲突"。当前 3/5 项能力已可实际使用（主动对话、行为画像、盘中异动推送）。修复 `l3.behavior_profile` 表结构冲突（P0，0.5天工作量）即可打通完整闭环。**

---

*核查基于 `6a61c01` 代码库，参考文件覆盖 l3_dialog_engine.py(809行), audit_analytics.py(300行), backtest_engine.py(830行), schedule_runner.py(1130+行), l3_phase_a.sql(161行) | 2026-05-30 第二次复核*