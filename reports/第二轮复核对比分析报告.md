# InvestPilot 系统 — 第二轮复核对比分析报告

**报告日期**: 2026-05-30  
**复核范围**: 第一轮复核（基于 commit `30831e7`）→ 第二轮复核（基于 commit `101fe4f`）  
**新增提交**: 4 个（`4807a95` → `64f874d` → `dc3bf1e` → `101fe4f`）  
**本地分支**: `master`，与 `origin/master` 同步（HEAD == origin/master）  
**GitHub 连接状态**: 网络暂时不可达（Connection reset），本地代码确认已是最新  

---

## 一、两轮复核之间的变更摘要

### 1.1 新增提交

| Commit | 描述 | 文件变更 |
|--------|------|---------|
| `4807a95` | 新增: 全面复核与实施总结报告 (v2.0 Phase T4 完成) | 新增 `reports/全面复核与实施总结报告.md` (270行) |
| `64f874d` | 更新: 全面实施总结报告_v2 — Phase T1-T4 88.6%完成度确认 | 新增 `reports/全面实施总结报告_v2.md` (209行) |
| `dc3bf1e` | **Phase 5 三大更新**: Agent接口统一 + 技能固化激活 + 回测引擎 | `backtester.py`(+702), `schedule_runner.py`(+94), `skill_library.py`(+6/-), `tamf_updater.py`(+35/-) |
| `101fe4f` | fix: job_weekly_backtest 改为查询实际持仓(46只),排除基金(.OF) | `backtester.py` 小幅修正 |

### 1.2 变更文件统计

| 文件 | 变更类型 | 行数变化 |
|------|---------|---------|
| `scripts/backtester.py` | **新增** | +702 行 |
| `scripts/schedule_runner.py` | 修改 | +94 行 |
| `scripts/tamf_updater.py` | 修改 | +35 / -29 行 |
| `scripts/skill_library.py` | 修改 | +6 行 |
| `reports/全面复核与实施总结报告.md` | 新增 | +270 行 |
| `reports/全面实施总结报告_v2.md` | 新增 | +209 行 |

---

## 二、关键问题对比分析（逐项验证）

### 2.1 Agent 接口层集成 — ✅ 已解决

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| `run_analysis.py` 是否使用 Agent 接口 | ❌ 未集成，仍直接使用 `llm_caller` | ✅ 已集成：`from agent_interface import get_agent` (L45)，`agent = get_agent()` (L410)，`agent.chat()` (L415) |
| `tamf_updater.py` 是否使用 Agent 接口 | ❌ 直接调用 `llm_caller` | ✅ 已统一：`get_agent().chat(prompt, system=system, force_model="deepseek")` (L955)，`force_model="ollama"` (L966) |
| `skill_library.py` 是否使用 Agent 接口 | ❌ 直接调用 `llm_caller` | ✅ 已统一：`from agent_interface import get_agent` (L71/L432)，`agent = get_agent()` (L112/L433) |

**结论**: Agent 接口层已完成三层统一接入（`run_analysis.py` / `tamf_updater.py` / `skill_library.py`），消除了对 `llm_caller` 的直接依赖。`run_analysis.py` 仍保留 `from llm_caller import estimate_cost, _parse_llm_response` 用于成本估算与响应解析（非 LLM 调用），属合理保留。

**遗留问题**: `agent_interface.py` 第 13 行硬编码 Linux 路径 `sys.path.insert(0, "/home/aileo/invest_system/scripts")`，在 Windows 系统上为无效路径（无副作用但不规范）。

---

### 2.2 技能固化（skills/ 目录）— ❌ 声称已激活但实际未创建

这是本轮复核发现的**最大偏差**。

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| `skills/` 根目录 | ❌ 不存在 | ❌ **仍不存在** |
| `skills/approved/` 子目录 | ❌ 不存在 | ❌ **仍不存在** |
| `skills/drafts/` 子目录 | ❌ 不存在 | ❌ **仍不存在** |
| 任何 `.md` 技能文件 | ❌ 不存在 | ❌ **仍不存在** |
| 代码框架 | ✅ `skill_library.py` 定义 `SKILLS_DIR/APPROVED_DIR/DRAFT_DIR` | ✅ 不变，且已接入 `agent_interface` |

**commit `dc3bf1e` 声称的内容**:
> "技能固化激活: skills/approved/ 目录建立, 首批2个技能已批准: earnings_analysis (财报业绩分析, deepseek路由), position_check (持仓快速诊断, ollama路由), RouterAgent._reload_skills() 验证通过, 技能匹配正常"

**实际代码库状态**:
- `c:\PythonProject\invest_system\skills\` → **不存在**（`Test-Path` 三目录均返回 `False`）
- `skills/approved/` 和 `skills/drafts/` 被 `.gitignore` 第 37-38 行排除
- `RouterAgent._reload_skills()` 在 `agent_interface.py:155-167` 确实存在，扫描 `APPROVED_DIR.glob("*.md")`
- `RouterAgent._match_skill()` 在 `agent_interface.py:169-174` 确实存在，用正则匹配触发关键词
- 但因为没有目录和文件，这些代码无法正常工作

**偏差分析**:
- 可能原因：技能目录在原开发环境（Linux `/home/aileo/invest_system/skills/approved/`）中创建并测试通过，但因 `.gitignore` 排除规则，**目录和文件无法提交到 Git**，pull 到 Windows 后自然丢失
- 这属于 **Git 管理策略缺陷**：`.gitignore` 不应该排除 `skills/approved/`，因为已批准的技能文件应当纳入版本控制
- 类似 `target_memories/` 的 `.gitignore` 问题已在 T4.4 修正过（移除了排除规则），但 `skills/` 目录的排除规则未被修正

---

### 2.3 回测引擎 — ✅ 已实现

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| `backtester.py` 文件 | ❌ 不存在 | ✅ 702 行，完整实现 |
| 事件驱动撮合引擎 | ❌ 未实现 | ✅ `BacktestEngine` 类 |
| 策略支持 | ❌ 未实现 | ✅ MA交叉 / RSI / MACD 三种策略 |
| 佣金+印花税+滑点 | ❌ 未实现 | ✅ 完整建模 |
| 绩效指标 | ❌ 未实现 | ✅ 总收益/年化/Sharpe/最大回撤/胜率/盈亏比 |
| 定时任务集成 | ❌ 未注册 | ✅ `job_weekly_backtest` 每周五 22:00 |

**结论**: 回测引擎已从"未完成项"变为"已完成项"。`全面实施总结报告_v2.md` 中标记为"🟡 中-未实现"的回测引擎错误需要更新。

---

### 2.4 Dashboard 密码保护 — ✅ 已解决

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| 密码保护机制 | ❌ 不存在 | ✅ 已实现：`_get_dashboard_password()` (L40)，`_check_auth()` (L57)，`st.text_input("请输入访问密码", type="password")` (L67) |
| 凭据存储 | — | 从 `~/.hermes/invest_credentials/store.json` 读取 `DASHBOARD_PASSWORD` |

**结论**: 仪表盘密码保护已实现，属 session 级一次认证机制。但仍需用户在 `store.json` 中手动配置 `DASHBOARD_PASSWORD` 字段。

---

### 2.5 Dashboard 模块化 — ❌ 未实施（但非阻塞）

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| 文件行数 | ~1662 行 | **1415 行**（减少了 ~247 行？可能是重构优化） |
| 文件大小 | ~71 KB | 71.25 KB（基本不变） |
| 模块拆分 | 未拆分 | 仍为单体文件 |

**结论**: Dashboard 仍未模块化，但这是一个"改进建议"级别的问题，不影响功能。1415 行的行数变化可能是代码重构/去重。

---

### 2.6 向量嵌入检索激活 — ❌ 仍未激活

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| `embedding_service.py` 代码 | ✅ 完整实现 | ✅ 不变 |
| `run_analysis.py` 中调用 | ❌ 未调用 | ❌ 仍未被主流水线调用 |
| `prompt_builder.py` 中调用 | ❌ 未集成 | ❌ 仍未集成向量检索结果 |

**结论**: `embedding_service.py` 提供了 `embed_news_articles()`、`search_similar_news()` 等完整功能，但主分析流水线 `run_analysis.py` 和 `prompt_builder.py` 均未实际调用这些函数。向量嵌入检索的"端到端激活"仍是一个待办事项。

---

### 2.7 数据备份与恢复 — ❌ 仍不存在

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| `data/backup*` 目录 | ❌ 不存在 | ❌ 仍不存在 |

---

### 2.8 TAMF 影子模式 / 验证记录 — ❌ 仍未实施

| 维度 | 第一轮复核 | 第二轮复核 |
|------|-----------|-----------|
| 影子模式对比 | ❌ 未找到 | ❌ 仍未实施 |
| TAMF 自动验证 | ❌ 未找到 | ❌ 仍未实施 |

---

### 2.9 关键数字对比

| 指标 | 第一轮复核 | 第二轮复核 | 变化 |
|------|-----------|-----------|------|
| TAMF .md 文件 | 46 | **47** | +1（TEMPLATE.md 计入） |
| 定时任务数 | 11 | **12** | +1（job_weekly_backtest） |
| Dashboard 行数 | ~1662 | **1415** | -247（重构优化） |
| 白名单路由规则 | 14 | 14 | 不变 |
| `skills/` 目录 | 不存在 | 不存在 | **无变化（声称已激活）** |
| `backtester.py` | 不存在 | **702 行** | 新增 |
| Agent 集成 run_analysis.py | 未集成 | **已集成** | 已解决 |

---

## 三、新发现的问题

| # | 问题 | 严重度 | 描述 |
|---|------|:---:|------|
| **N1** | **`skills/` 目录被 .gitignore 排除导致技能文件无法同步** | 🔴 高 | `.gitignore` 第 37-38 行排除 `skills/approved/` 和 `skills/drafts/`，导致即使原环境创建了技能文件，Git 也无法追踪。需移除这两个排除规则，并创建目录及示例 `.md` 文件。commit `dc3bf1e` 声称"首批2个技能已批准"（`earnings_analysis`、`position_check`），但这些技能文件在代码库中不存在。 |
| **N2** | **`全面实施总结报告_v2.md` 的数据已过时** | 🟡 中 | 该报告标记"回测引擎: 未实现"（🟡 中），但 Phase 5 已实现。报告标记"半自动 Skill 固化: 未激活"（🟡 中），现在代码已统一 Agent 接口但技能目录仍为空。 |
| **N3** | **`agent_interface.py` 硬编码 Linux 路径** | 🟡 中 | `sys.path.insert(0, "/home/aileo/invest_system/scripts")` 硬编码 Linux 路径，在 Windows 11 上为无效路径。应改为相对路径或条件判断。 |
| **N4** | **向量嵌入检索仍未被主流水线调用** | 🟡 中 | `embedding_service.py` 提供了完整功能但未集成到 `run_analysis.py` 或 `prompt_builder.py` 中，影响分析质量。 |
| **N5** | **Dashboard 行数从 1662 降到 1415 行** | 🟢 低 | 减少 ~247 行，可能是代码优化去重，但需要确认是否有功能被误删。账面密码保护功能已加入，行数反降说明之前可能有冗余代码。 |

---

## 四、上一轮问题解决进展汇总

| 上一轮问题 | 状态 | 本轮验证 |
|-----------|:---:|------|
| Agent 接口层未集成到 run_analysis.py | ✅ **已解决** | run_analysis.py L45/L410/L415 已集成 |
| tamf_updater.py 直接调用 llm_caller | ✅ **已解决** | 改为 `get_agent().chat(force_model=...)` |
| skill_library.py 直接调用 llm_caller | ✅ **已解决** | 改为 `get_agent().chat()` |
| skills/ 目录为空 | ❌ **未解决** | `.gitignore` 排除 + 目录不存在 |
| Dashboard 密码保护 | ✅ **已解决** | `_get_dashboard_password()` + session 认证 |
| 回测引擎未实现 | ✅ **已解决** | `backtester.py` 702 行 + 定时任务 |
| Dashboard 模块化 | ⏳ 未实施 | 1415 行单体文件 |
| 向量嵌入检索激活 | ⏳ 未激活 | `run_analysis.py` 未调用 |
| 数据备份恢复 | ❌ 未实施 | 无备份机制 |
| TAMF 影子模式 | ❌ 未实施 | 无验证记录 |
| 定时任务与报告声称不一致（11 vs 声称） | ✅ **已解决** | 现为 12 个（+1 backtest） |

**解决率**: 6/11 已解决 (54.5%)，3/11 未解决，2/11 未实施。

---

## 五、系统整体状态评估

### 5.1 各维度评估

| 维度 | 第一轮评分 | 第二轮评分 | 变化理由 |
|------|:---:|:---:|------|
| 数据采集层 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 不变 |
| 定时调度层 | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐⭐ | +backtest 任务 |
| 数据持久化层 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 不变 |
| Agent 集成层 | ⭐⭐⭐☆☆ | ⭐⭐⭐⭐⭐ | +run/tamf/skill 三处统一 |
| LLM 路由层 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 不变 |
| Prompt 组装层 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 不变 |
| TAMF 记忆系统 | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐☆ | 不变 |
| 技能固化 | ⭐☆☆☆☆ | ⭐⭐☆☆☆ | Agent 统一但目录仍空 |
| 回测引擎 | ⭐☆☆☆☆ | ⭐⭐⭐⭐☆ | 新增功能 |
| 安全体系 | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐⭐ | +密码保护 |
| Web 仪表盘 | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐☆ | 不变 |
| 文档报告 | ⭐⭐⭐☆☆ | ⭐⭐⭐☆☆ | 不变 |

### 5.2 完成度评估

- **Phase T1-T4**: 31/35 = 88.6%（不变）
- **Phase 5**: 2/3 已落实（Agent 接口统一 ✅，回测引擎 ✅，技能固化目录 ⚠️ 代码就绪但目录缺失）
- **总完成度**: ~90%（Agent 接口统一和回测引擎落地抵消了技能目录缺失的影响）

---

## 六、优先级行动项建议

| 优先级 | 行动项 | 说明 |
|:---:|------|------|
| 🔴 P0 | **修正 `.gitignore` 并创建 `skills/` 目录** | 移除 `skills/approved/`、`skills/drafts/` 的排除规则；创建目录结构；如果原环境有 `earnings_analysis.md` 和 `position_check.md`，需手动迁移到仓库并提交 |
| 🔴 P0 | **创建 skills/approved/ 目录及首批技能文件** | 即使 AI 代生成，也需有实际的 `.md` 文件才能使 `RouterAgent._reload_skills()` 正常工作 |
| 🟡 P1 | **移除 `agent_interface.py` 硬编码 Linux 路径** | `sys.path.insert(0, "/home/aileo/invest_system/scripts")` 改为相对路径 |
| 🟡 P1 | **更新 `全面实施总结报告_v2.md`** | 回测引擎状态从"未实现"改为"已实现"；技能固化从"未激活"更新为"目录待创建" |
| 🟡 P1 | **激活向量嵌入检索** | 在 `run_analysis.py` 或 `prompt_builder.py` 中集成 `search_similar_news()` |
| 🟢 P2 | **Dashboard 模块化** | 将 1415 行的单体文件拆分为多模块 |
| 🟢 P2 | **TAMF 影子模式** | 实施 shadow mode 验证机制 |

---

*第二轮复核报告由 Hermes Agent 基于 4 个新 commit 的实际代码库交叉验证生成 | 2026-05-30*